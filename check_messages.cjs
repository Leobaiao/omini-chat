const sql = require(\"mssql\");\n\nconst config = {\n    user: \"sa\",\n    password: \"Intamr17@\",\n    server: \"localhost\",\n    port: 14333,\n    database: \"OmniChatDev\",\n    options: {\n        encrypt: false,\n        trustServerCertificate: true\n    }\n};\n\nasync function run() {\n    try {\n        await sql.connect(config);\n        const r = await sql.query(\"SELECT TOP 10 * FROM omni.Message ORDER BY CreatedAt DESC\");\n        console.log(\"Recent Messages:\", JSON.stringify(r.recordset, null, 2));\n        \n        const rConv = await sql.query(\"SELECT TOP 5 * FROM omni.Conversation ORDER BY LastMessageAt DESC\");\n        console.log(\"Recent Conversations:\", JSON.stringify(rConv.recordset, null, 2));\n\n        await sql.close();\n    } catch (err) {\n        console.error(err);\n    }\n}\n\nrun();\n
